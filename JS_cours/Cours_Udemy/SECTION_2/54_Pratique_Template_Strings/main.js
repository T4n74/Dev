const perso = {
//	nom: "chouchou",  
//si un oublie de nom
	force: 20,
	arme "fatal"
};

// const texte = perso.nom + " a une force de " + perso.force + " et une arme " + perso.arme + ".";
const texte = `${perso.nom ? perso.nom : "Alban"} est mon nom.`;

console.log(texte);