 /* Maj pour la premiere lettre car natif au langage
 Pour que sa soit prit pour un des a 6 face */
var random = Math.floor(Math.random() * 6 + 1);  // floor c'est pour arrondir || + 1 pour ne pas afficher 0

console.log(random);